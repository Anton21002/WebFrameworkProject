{{!-- Extend the main layout --}}
{{#extend "layout/main"}}
  {{!-- Set the title --}}
  {{#content "title"}}Add Clothe{{/content}}
  {{!-- Define the page content --}}
  {{#content "body"}}
<h1>Clothes List</h1>

{{#if clothes}}
  <table>
    <thead>
      <tr>
        <th>Name</th>
        <th>Type</th>
        <th>Style</th>
        <th>Sizes</th>
        <th>Material</th>
        <th>Brand</th>
        <th>Primary Color</th>
        <th>Price (Euro)</th>
        <th>On Discount</th>
      </tr>
    </thead>
    <tbody>
      {{#each clothes}}
        <tr>
          <td tabindex="0">{{cloName}}</td>
          <td tabindex="0">{{cloType}}</td>
          <td tabindex="0">{{cloStyle}}</td>
          <td tabindex="0">{{cloSizes}}</td>
          <td tabindex="0">{{cloMat}}</td>
          <td tabindex="0">{{cloBrand}}</td>
          <td tabindex="0">{{cloColorPrim}}</td>
          <td tabindex="0">{{priceEuro}}</td>
          <td tabindex="0">{{#if onDiscount}}Yes{{else}}No{{/if}}</td>
          <td><a href="/clothes/{{_id}}/edit" tabindex="0">Edit</a></td>
          <td>
            <form method="POST" action="/clothes/{{_id}}?_method=DELETE">
              <button type="button" tabindex="0">Delete</button>
            </form>
          </td>
        </tr>
      {{/each}}
    </tbody>
  </table>
{{else}}
  <p>No clothes to display</p>
{{/if}}

<a href="/clothes/new" tabindex="0">Add New Clothe</a>
